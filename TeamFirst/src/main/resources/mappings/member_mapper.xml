<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- users_mapper.xml -->
<mapper namespace="co.team.security.service.impl.MemberMapper">

	<!-- ADMIN select 리스트 -->
	<select id="getMemberById" resultType="memberVO"
		parameterType="memberVO">

		select id, password, mem_reg_id from allid where id =#{id}

	</select>

	<select id="getMax" resultType="int" parameterType="memberVO">

		SELECT count(*) FROM allid

	</select>
	<select id="rememberId" resultType="memberVO"
		parameterType="memberVO">

		select id from allid where name =#{name} and phone_number=#{phone_number}

	</select>
	<select id="getEmail" resultType="memberVO"
		parameterType="memberVO">

		select email from allid where id =#{id}

	</select>

</mapper>
